query GetUserProfile($id: Int) {
    User(id: $id) {
        id
        name
        donatorBadge
        donatorTier
        previousNames {
            name
        }
        about(asHtml: true)
        avatar {
            large
            medium
        }
        bannerImage
        createdAt
        updatedAt
        siteUrl
    }
}