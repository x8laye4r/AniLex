query GetUserFavoriteStudioQuery($userId: Int, $page: Int, $perPage: Int) {
    User(id: $userId) {
        favourites {
            studios {
                nodes {
                    id
                    name
                    isAnimationStudio
                }
                pageInfo {
                    ...PageInfo
                }
            }
        }
    }
}